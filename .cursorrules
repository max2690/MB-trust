# Cursor Rules для MB-TRUST

## 🎯 ПРАВИЛА РАБОТЫ С ДИЗАЙНОМ

### ❌ НЕ МЕНЯТЬ (ЗАФИКСИРОВАННЫЕ КОМПОНЕНТЫ):
- `src/components/ui/*` - Все UI компоненты зафиксированы
- `src/app/globals.css` - Глобальные стили зафиксированы
- `tailwind.config.ts` - Конфигурация Tailwind зафиксирована

### ✅ МОЖНО МЕНЯТЬ (ЛОГИКА И ДАННЫЕ):
- `src/app/*/page.tsx` - Страницы (только логика, не дизайн)
- `src/app/api/*` - API routes
- `src/lib/*` - Утилиты и логика
- `prisma/*` - База данных

## 🚫 ЗАПРЕЩЕНО:
- Изменять Tailwind классы в UI компонентах
- Менять цвета, spacing, анимации
- Ломать layout и композицию
- Добавлять новые CSS классы в UI компоненты

## ✅ РАЗРЕШЕНО:
- Менять props и handlers в UI компонентах
- Добавлять логику в страницы
- Создавать новые API endpoints
- Работать с базой данных

## 📝 ПРИМЕРЫ:

### ✅ Правильно:
```tsx
// Меняем только логику
<Button onClick={handleSubmit} disabled={isLoading}>
  {isLoading ? 'Загрузка...' : 'Отправить'}
</Button>
```

### ❌ Неправильно:
```tsx
// НЕ МЕНЯТЬ className в UI компонентах
<Button className="bg-red-500"> // ЗАПРЕЩЕНО!
```

## 🎨 ДИЗАЙН:
- Все визуальные изменения делаются вручную
- Cursor работает только с логикой и данными
- UI компоненты остаются неизменными

